# Link to control plane

This radar is a research / evaluation kit.

It does **not** auto-feed the agent-control-plane repo.

**Manual link (for you):**
- When the Platform Watcher flags a material release, update `RELEASES.md` and the ranking on `main`.
- If a platform choice affects your agent mesh, note it in `agent-control-plane` yourself.

No automatic sync between the two repos. That keeps this one simple and private-friendly.
